export default function Logo({ className = '' }) {
  return (
    <div className={`font-sora font-extrabold tracking-tight ${className}`}>
      <span className="text-white">rev</span>
      <span className="text-electric">i</span>
    </div>
  )
}
